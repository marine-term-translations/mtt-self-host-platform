# Organization setup
GITEA_ORG_NAME=marine-org
GITEA_ORG_FULL_NAME=Marine Organization
GITEA_ORG_DESCRIPTION=Organization for marine term translations
GITEA_ORG_EMAIL=org@example.com
GITEA_ORG_LOCATION=Earth
GITEA_ORG_WEBSITE=https://example.com
GITEA_ORG_VISIBILITY=public
GITEA_DB_PASS=choose_strong_password


GITEA_ADMIN_TOKEN= #To be obtained after first run setup, see README
RUNNER_TOKEN= #To be obtained after first run setup, see README
RUNNER_NAME= #To be set to the name of the runner registered in Gitea

# Gitea admin setup
GITEA_ADMIN_USER=admin
GITEA_ADMIN_PASS=adminpass
GITEA_ADMIN_EMAIL=admin@example.com

# Gitea organization setup
GITEA_ORG_NAME=marine-org

# Frontend environment variables (VITE_ prefix required)
# Note: These are used by the USER'S BROWSER (not the frontend container)
# The browser runs outside Docker and needs to access services via localhost or public domain
# For container-to-container communication, Docker uses internal network names (e.g., http://backend:5000)
VITE_API_URL=http://localhost:5000/api
VITE_GITEA_URL=http://localhost:3000

# Optional overrides only needed in production:
# In production, change these to your public domain so browsers can reach your services
# DOMAIN=emobon-kb.web.vliz.be
# ROOT_URL=https://emobon-kb.web.vliz.be/
# VITE_API_URL=https://emobon-kb.web.vliz.be/api
# VITE_GITEA_URL=https://emobon-kb.web.vliz.be/

# Translation DB repo integration
TRANSLATIONS_REPO=translations-data
TRANSLATIONS_REPO_PATH=backend/translations-data
SQLITE_DB_PATH=backend/translations-data/translations.db
