# Node Environment (set to 'development' for local testing, 'production' for deployment)
NODE_ENV=development

# ORCID OAuth Configuration
ORCID_CLIENT_ID=APP-XXXXXXXXXXXXXXXX
ORCID_CLIENT_SECRET=11111111-2222-3333-4444-555555555555
SESSION_SECRET=super-long-random-string-here

# Backend URLs (development defaults shown, update for production)
BASE_URL=http://localhost:5000
FRONTEND_URL=http://localhost:5173

# For production with HTTPS, use:
# BASE_URL=https://mtt.vliz.be
# FRONTEND_URL=https://mtt.vliz.be
# NODE_ENV=production

# Openrouter API key for AI-suggested translations (OPTIONAL)
# Users can now provide their own API key in Settings
# This acts as a fallback if users haven't configured their own key
# Get your key from: https://openrouter.ai/settings/keys
VITE_OPENROUTER_API_KEY=your_openrouter_api_key_here

# Frontend environment variables (VITE_ prefix required)
# Note: These are used by the USER'S BROWSER (not the frontend container)
VITE_API_URL=http://localhost:5000/api

# Production configuration example for mtt.vliz.be:
# VITE_API_URL=https://mtt.vliz.be/api
# VITE_DOMAIN=mtt.vliz.be
# VITE_ROOT_URL=https://mtt.vliz.be

# Translation Database Configuration
# The database will be automatically created on startup if it doesn't exist
SQLITE_DB_PATH=backend/data/translations.db

# GraphDB Configuration (optional - for LDES and RDF data)
# Customize heap size based on your environment (default: -Xms8g -Xmx16g)
# GDB_JAVA_OPTS=-Xms4g -Xmx8g -Dcom.ontotext.graphdb.monitoring.jmx=true
# GDB_REPO=kgap
# GDB_HOME_FOLDER=/opt/graphdb/home/

# GraphDB Connection (for SPARQL queries)
GRAPHDB_URL=http://localhost:7200
GRAPHDB_REPO=kgap

# Docker Compose Configuration
# COMPOSE_PROJECT_NAME=kgap
